import { z } from 'zod';
import { NewCRMUserInfo } from '@/interfaces/crm';

function createNewCRM(input: NewCRMUserInfo) {
  // TODO: Create the new data all in the database
  //       work on the database schema now...
  //       validate all data second time to make sure the data is correct
}

export { createNewCRM };
